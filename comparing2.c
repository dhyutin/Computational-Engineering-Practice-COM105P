#include <stdio.h>
void main()
{
int a,b;
printf("Enter the values of a and b  ");
scanf("%d %d",&a,&b);
if(a>b)
   {
   printf("a is the larger number.");
   }
   else
   {
   printf("b is the larger number.");
   }
}
